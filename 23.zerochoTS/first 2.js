"use strict";
const a = "5";
const b = 5;
const c = true;
const d = undefined;
const e = null;
function add(x, y) {
    return x + y;
}
const add2 = (x, y) => x + y;
const add3 = (x, y) => x + y;
const add4 = (x, y) => x + y;
const arr = ["123", "456"]; // 배열의 타입 선언
const arr2 = [123, 456]; // 제네릭을 통한 배열의 타입 선언
const arr3 = [123, 456, "hello"]; // 튜플(길이가 정해진 배열)의 타입 선언
const obj = { lat: 37.5, lon: 127.5 }; // 객체의 타입 선언
console.log(3 /* EDirection.Right */);
